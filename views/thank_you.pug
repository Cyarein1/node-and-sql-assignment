doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Thank You
    link(href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css", rel="stylesheet")
  body
    div(class="bg-gray-100 min-h-screen py-6 flex flex-col justify-center sm:py-12")
      div(class="relative py-3 sm:max-w-xl sm:mx-auto")
        div(class="relative px-4 py-10 bg-white mx-8 md:mx-0 shadow rounded-3xl sm:p-10")
          h1(class="text-center font-semibold text-2xl uppercase mb-6") Thank You for Your Submission
          p(class="text-center text-lg mb-8") The following was saved to the database:
          if visitorData
            ul(class="text-left")
              each value, key in visitorData
                li(class="mb-3")
                  span(class="font-semibold") #{key}: 
                  span #{value}
          div(class="text-center mt-8")
            a(href="/new_visitor" class="inline-block px-6 py-2 bg-blue-500 text-white font-semibold rounded-md shadow-md hover:bg-blue-600 transition duration-300") Back to Form

